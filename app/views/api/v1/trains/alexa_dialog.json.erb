{
  "version": "1.0",
  "shouldEndSession": false,
  "sessionAttributes": {},
  "response": {
    "outputSpeech": {
      "type": "SSML",
      "ssml": "<%= raw @conn[:response][:ssml] %>"
    },
    "directives": [
      {
        "type": "Dialog.ElicitSlot",
        "slotToElicit": "<%= raw @conn[:response][:slot_to_elicit] %>",
        "updatedIntent": {
          "name": "StationDirect",
          "confirmationStatus": "NONE",
          "slots": {
            "station_id": {
              "name": "station_id",
              "confirmationStatus": "NONE"
              <% if @conn[:response][:slots][:station_id].present? %>
                , "value": "<%= raw @conn[:response][:slots][:station_id] %>"
              <% end %>
            },
            "direction": {
              "name": "direction",
              "confirmationStatus": "NONE"
              <% if @conn[:response][:slots][:direction].present? %>
                , "value": "<%= raw @conn[:response][:slots][:direction] %>"
              <% end %>
            }
          }
        }
      }
    ]
  }
}
